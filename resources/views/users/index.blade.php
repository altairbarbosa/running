<x-layouts.app title="Usuários e acessos">
    @php($roleLabels = ['admin' => 'Administrador', 'trainer' => 'Professor', 'member' => 'Aluno'])
    <div class="mb-6 flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><form class="flex flex-1 flex-col gap-2 sm:flex-row"><input class="field max-w-md" name="search" value="{{ request('search') }}" placeholder="Buscar nome ou e-mail"><select class="field max-w-48" name="role"><option value="">Todos os perfis</option>@foreach($roleLabels as $value => $label)<option value="{{ $value }}" @selected(request('role') === $value)>{{ $label }}</option>@endforeach</select><button class="btn-secondary">Filtrar</button></form><a class="btn-primary" href="{{ route('users.create') }}">+ Novo usuário</a></div>
    <div class="card overflow-x-auto"><table class="w-full min-w-[760px] text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-6 py-4">Usuário</th><th class="px-6 py-4">Perfil de acesso</th><th class="px-6 py-4">Contato</th><th class="px-6 py-4">Status</th><th class="px-6 py-4"></th></tr></thead><tbody class="divide-y divide-slate-100">
        @forelse($users as $user)<tr><td class="px-6 py-4"><div class="flex items-center gap-3"><x-avatar :user="$user" size="sm"/><div><p class="font-bold text-ink-950">{{ $user->name }}</p><p class="text-xs text-slate-400">#{{ $user->id }}</p></div></div></td><td class="px-6 py-4"><span class="rounded-full bg-brand-50 px-2.5 py-1 text-xs font-bold text-brand-700">{{ $roleLabels[$user->role] ?? $user->role }}</span></td><td class="px-6 py-4"><p>{{ $user->email }}</p><p class="text-xs text-slate-500">{{ $user->phone }}</p></td><td class="px-6 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-bold {{ $user->active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-500' }}">{{ $user->active ? 'Ativo' : 'Inativo' }}</span></td><td class="px-6 py-4 text-right"><a class="font-semibold text-brand-700" href="{{ route('users.edit', $user) }}">Editar →</a></td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-slate-500">Nenhum usuário encontrado.</td></tr>@endforelse
    </tbody></table></div><div class="mt-6">{{ $users->links() }}</div>
</x-layouts.app>
