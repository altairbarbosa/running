<x-layouts.app title="Treinos">
    @if(auth()->user()->isStaff())<div class="mb-6 flex justify-end"><a class="btn-primary" href="{{ route('workouts.create') }}">+ Elaborar treino</a></div>@endif
    <div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($workouts as $workout)<a href="{{ route('workouts.show', $workout) }}" class="card group p-6 transition hover:-translate-y-0.5 hover:shadow-md"><div class="flex justify-between"><span class="rounded-full bg-brand-50 px-2.5 py-1 text-xs font-bold text-brand-700">{{ $workout->items->count() }} exercícios</span><span class="text-xs text-slate-400">{{ $workout->starts_at->format('d/m/Y') }}</span></div><h2 class="mt-5 text-xl font-black text-ink-950 group-hover:text-brand-700">{{ $workout->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $workout->member->name }}</p><p class="mt-5 text-sm font-semibold text-brand-700">Abrir ficha →</p></a>@empty<p class="col-span-full py-12 text-center text-slate-500">Nenhum treino elaborado.</p>@endforelse</div><div class="mt-6">{{ $workouts->links() }}</div>
</x-layouts.app>
