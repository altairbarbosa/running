<x-layouts.app title="Exercícios">
    <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><form class="flex max-w-md flex-1 gap-2"><input class="field" name="search" value="{{ request('search') }}" placeholder="Buscar exercício"><button class="btn-secondary">Buscar</button></form><a class="btn-primary" href="{{ route('exercises.create') }}">+ Novo exercício</a></div>
    <div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">@forelse($exercises as $exercise)<article class="card p-5"><div class="flex items-start justify-between gap-4"><div><p class="font-bold text-ink-950">{{ $exercise->name }}</p><p class="mt-1 text-sm text-brand-700">{{ $exercise->muscle_group ?: 'Grupo não informado' }}</p></div><span class="size-2 rounded-full {{ $exercise->active ? 'bg-emerald-500' : 'bg-slate-300' }}"></span></div><p class="mt-4 line-clamp-2 min-h-10 text-sm text-slate-500">{{ $exercise->instructions ?: 'Sem instruções adicionais.' }}</p><a class="mt-4 inline-block text-sm font-semibold text-brand-700" href="{{ route('exercises.edit', $exercise) }}">Editar →</a></article>@empty<p class="col-span-full py-12 text-center text-slate-500">Nenhum exercício encontrado.</p>@endforelse</div><div class="mt-6">{{ $exercises->links() }}</div>
</x-layouts.app>
