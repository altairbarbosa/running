<x-layouts.app title="Alunos">
    <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><form class="flex max-w-md flex-1 gap-2"><input class="field" name="search" value="{{ request('search') }}" placeholder="Buscar por nome ou e-mail"><button class="btn-secondary">Buscar</button></form><a class="btn-primary" href="{{ route('members.create') }}">+ Novo aluno</a></div>
    <div class="card overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-6 py-4">Aluno</th><th class="px-6 py-4">Contato</th><th class="px-6 py-4">Status</th><th class="px-6 py-4"></th></tr></thead><tbody class="divide-y divide-slate-100">
        @forelse($members as $member)<tr><td class="px-6 py-4"><div class="flex items-center gap-3"><x-avatar :user="$member" size="sm"/><div><p class="font-semibold text-ink-950">{{ $member->name }}</p><p class="text-xs text-slate-500">{{ $member->birth_date?->format('d/m/Y') ?? 'Nascimento não informado' }}</p></div></div></td><td class="px-6 py-4"><p>{{ $member->email }}</p><p class="text-xs text-slate-500">{{ $member->phone }}</p></td><td class="px-6 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-bold {{ $member->active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-500' }}">{{ $member->active ? 'Ativo' : 'Inativo' }}</span></td><td class="px-6 py-4 text-right"><a class="font-semibold text-brand-700 hover:underline" href="{{ route('members.edit', $member) }}">Editar</a></td></tr>@empty<tr><td colspan="4" class="px-6 py-12 text-center text-slate-500">Nenhum aluno encontrado.</td></tr>@endforelse
    </tbody></table></div><div class="mt-6">{{ $members->links() }}</div>
</x-layouts.app>
