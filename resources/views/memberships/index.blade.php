<x-layouts.app title="Matrículas">
    <div x-data="{ modal: @js((bool) old('membership_modal') || request('modal') === 'create'), open(){this.modal=true;document.body.classList.add('overflow-hidden')}, close(){this.modal=false;document.body.classList.remove('overflow-hidden')} }" x-init="if(modal) document.body.classList.add('overflow-hidden')" @keydown.escape.window="close()">
        <div class="mb-6 flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><form class="flex flex-1 flex-col gap-2 sm:flex-row"><input class="field max-w-md" name="search" value="{{ request('search') }}" placeholder="Buscar aluno"><select class="field max-w-48" name="status"><option value="">Todos os status</option><option value="active" @selected(request('status') === 'active')>Ativas</option><option value="paused" @selected(request('status') === 'paused')>Pausadas</option><option value="cancelled" @selected(request('status') === 'cancelled')>Canceladas</option></select><button class="btn-secondary">Filtrar</button></form><button type="button" class="btn-primary" @click="open()"><x-icon name="plus" />Nova matrícula</button></div>
        <div class="card overflow-x-auto"><table class="w-full min-w-[760px] text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-6 py-4">Aluno</th><th class="px-6 py-4">Plano</th><th class="px-6 py-4">Início</th><th class="px-6 py-4">Valor</th><th class="px-6 py-4">Status</th><th class="px-6 py-4"></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($memberships as $membership)<tr><td class="px-6 py-4"><p class="font-bold text-ink-950">{{ $membership->member->name }}</p><p class="text-xs text-slate-500">Vencimento dia {{ $membership->billing_day }}</p></td><td class="px-6 py-4">{{ $membership->plan->name }}</td><td class="px-6 py-4">{{ $membership->starts_at->format('d/m/Y') }}</td><td class="px-6 py-4 font-semibold">R$ {{ number_format($membership->price, 2, ',', '.') }}</td><td class="px-6 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-bold {{ $membership->status === 'active' ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600' }}">{{ ['active' => 'Ativa', 'paused' => 'Pausada', 'cancelled' => 'Cancelada'][$membership->status] ?? $membership->status }}</span></td><td class="px-6 py-4 text-right"><a class="icon-btn" href="{{ route('memberships.show', $membership) }}" title="Ver detalhes" aria-label="Ver matrícula de {{ $membership->member->name }}"><x-icon name="eye" size="size-4"/></a></td></tr>@empty<tr><td colspan="6" class="px-6 py-12 text-center text-slate-500">Nenhuma matrícula encontrada.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $memberships->links() }}</div>

        <div x-show="modal" x-cloak class="fixed inset-0 z-50 overflow-y-auto" role="dialog" aria-modal="true"><div class="flex min-h-full items-end justify-center sm:items-center sm:p-6"><div class="fixed inset-0 bg-ink-950/65 backdrop-blur-sm" @click="close()"></div><form method="POST" action="{{ route('memberships.store') }}" class="relative w-full rounded-t-3xl bg-white shadow-2xl sm:max-w-2xl sm:rounded-3xl">@csrf<input type="hidden" name="membership_modal" value="create"><div class="flex items-start justify-between border-b border-slate-100 px-5 py-5 sm:px-7"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-brand-600">Novo cadastro</p><h2 class="mt-1 text-xl font-black text-ink-950">Nova matrícula</h2></div><button type="button" class="grid size-10 place-items-center rounded-xl text-slate-400 hover:bg-slate-100" @click="close()"><x-icon name="close"/></button></div><div class="max-h-[68vh] overflow-y-auto px-5 py-6 sm:px-7">@if($errors->any())<div class="mb-5 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800">Revise os dados informados.</div>@endif<div class="grid gap-5 sm:grid-cols-2"><div class="sm:col-span-2"><label class="label">Aluno</label><select class="field" name="member_id" required><option value="">Selecione o aluno</option>@foreach($members as $member)<option value="{{ $member->id }}" @selected(old('member_id') == $member->id)>{{ $member->name }} · {{ $member->email }}</option>@endforeach</select></div><div class="sm:col-span-2"><label class="label">Plano</label><select class="field" name="plan_id" required><option value="">Selecione o plano</option>@foreach($plans as $plan)<option value="{{ $plan->id }}" @selected(old('plan_id') == $plan->id)>{{ $plan->name }} · R$ {{ number_format($plan->price, 2, ',', '.') }}</option>@endforeach</select></div><div><label class="label">Data de início</label><input class="field" type="date" name="starts_at" value="{{ old('starts_at', today()->format('Y-m-d')) }}" required></div><div><label class="label">Dia de vencimento</label><input class="field" type="number" min="1" max="28" name="billing_day" value="{{ old('billing_day', 10) }}" required></div><div><label class="label">Término previsto</label><input class="field" type="date" name="ends_at" value="{{ old('ends_at') }}"></div><div class="sm:col-span-2"><label class="label">Observações</label><textarea class="field" name="notes" rows="3">{{ old('notes') }}</textarea></div></div><div class="mt-5 rounded-xl bg-brand-50 px-4 py-3 text-sm text-brand-700">As próximas cobranças serão geradas automaticamente.</div></div><div class="flex flex-col-reverse gap-3 border-t border-slate-100 px-5 py-4 sm:flex-row sm:justify-end sm:px-7"><button type="button" class="btn-secondary" @click="close()">Cancelar</button><button class="btn-primary">Confirmar matrícula</button></div></form></div></div>
    </div>
</x-layouts.app>
